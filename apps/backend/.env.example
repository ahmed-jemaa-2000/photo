# Server
HOST=0.0.0.0
PORT=1337

# Secrets
# IMPORTANT: Generate strong secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# APP_KEYS needs 4 comma-separated keys
APP_KEYS=<generate-4-keys-separated-by-commas>
API_TOKEN_SALT=<generate-secret>
ADMIN_JWT_SECRET=<generate-secret>
TRANSFER_TOKEN_SALT=<generate-secret>
JWT_SECRET=<generate-secret>

# Database
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=busi_dev
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_SSL=false

# Environment
NODE_ENV=development
